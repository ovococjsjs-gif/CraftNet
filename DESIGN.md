# CraftNet — дизайн-документ

Fabric-мод для **Minecraft 1.21.11**: «смартфон в Minecraft» — онлайн-магазин,
банк, биржа, GPS-карта, сотовая связь (2G/3G/4G), новые здания в деревнях,
вышки связи и рабочие места (завод / грузчик / повар / курьер).

## Версии (проверено по meta.fabricmc.net и maven.fabricmc.net, 2026-08-24)

| Компонент | Версия |
|---|---|
| Minecraft | 1.21.11 |
| Yarn | 1.21.11+build.6 |
| Fabric Loader | 0.19.3 |
| Fabric API | 0.141.6+1.21.11 |
| Fabric Loom | 1.17.15 |
| Gradle | 8.14.3 (совместим с Loom 1.17; можно поднять до 9.x) |
| Java | 21 |

## Ключевые факты API 1.21.11 (проверено по yarn-javadoc)

- `Item.Settings.registryKey(RegistryKey<Item>)` — обязателен.
- `AbstractBlock.Settings.create().registryKey(RegistryKey<Block>)` — обязателен.
- `Item.use(World, PlayerEntity, Hand)` возвращает **`ActionResult`** (не TypedActionResult).
- `Item.appendTooltip(ItemStack, Item.TooltipContext, TooltipDisplayComponent, Consumer<Text>, TooltipType)`.
- Персистентные данные мира: `PersistentState` (абстрактный, `markDirty()`),
  `PersistentStateType<T>` — record `(String id, Supplier<T>, Codec<T>, DataFixTypes)`,
  `PersistentStateManager.getOrCreate(type)`.
- Поиск структур: `ChunkGenerator.locateStructure(ServerWorld, RegistryEntryList<Structure>, BlockPos, int radius, boolean)`.
  Получение списка по тегу: `registry.getOrThrow(StructureTags.VILLAGE)`.
- GUI (новый рендерер 1.21.6+): `DrawContext.drawTexture(RenderPipeline, Identifier, x, y, u, v, w, h, tw, th)`,
  `drawText/drawTextWithShadow/drawCenteredTextWithShadow`, `fill`, `enableScissor`,
  `getMatrices()` → `Matrix3x2fStack` (JOML). Пайплайны: `net.minecraft.client.gl.RenderPipelines` (`GUI_TEXTURED`, `GUI`).
- Динамические текстуры: `NativeImageBackedTexture(Supplier<String>, w, h, useStb)` + `upload()`,
  `NativeImage.setColorArgb(x, y, 0xAARRGGBB)`, регистрация через `TextureManager.registerTexture(Identifier, AbstractTexture)`.
- Пулы структур: `StructurePool` — приватные списки `elements` и `elementWeights`;
  расширяем accessor-миксином. `StructurePoolElement.ofLegacySingle(String id)` → функция с `Projection.RIGID` для деревень.
- Папки датапаков (1.21+): `data/<ns>/structure/*.nbt`, `loot_table/`, `recipe/`.
- Клиентские модели предметов (1.21.4+): `assets/<ns>/items/<id>.json` → `{"model":{"type":"minecraft:model","model":"<ns>:item/<id>"}}`.

## Архитектура

```
net.craftnet
├── CraftNet                 — ModInitializer
├── CraftNetClient           — ClientModInitializer (сеть, GUI, кейбинды)
├── block/ModBlocks          — блоки: пвз-стойка, банк-терминал, станок завода,
│                              стойка кафе, ядро вышки, грузовой ящик
├── block/TowerCoreBlock     — хуки onPlaced/onStateReplaced → VillageManager
├── item/ModItems            — телефон, банкнота, грузовой ящик-предмет, пакет еды
├── item/PhoneItem           — открытие телефона
├── component/ModComponents  — DataComponent: номинал банкноты
├── econ/MoneyManager        — балансы игроков (persistent)
├── econ/PriceManager        — прайс-лист (таблица + эвристика fallback), спред
├── econ/StocksManager       — биржа: 5 компаний, random-walk, истории, портфели
├── orders/OrderManager      — заказы магазина: доставка в ПВЗ, выплаты за продажу
├── village/VillageManager   — обнаружение деревень, вышки, офлайн, уровни сигнала
├── village/VillageStructureInjector — инъекция NBT в пулы деревень (SERVER_STARTED)
├── network/ModPackets       — 3 generic-пейлоада: OpenScreen/ScreenSync/ScreenAction
├── network/ServerActions    — серверная обработка действий GUI
├── jobs/JobManager          — задания: завод/грузчик/повар/курьер, выплаты по лестнице
├── command/AdminCommands    — /craftnet money|signal|village|tower
└── mixin/StructurePoolAccessor
net.craftnet.client
├── NetHooks                 — client-ресиверы пакетов
├── Keybinds                 — клавиша P = телефон
├── gui/PhoneScreen          — телефон: Главная, Магазин, Биржа, GPS, Банк, Связь
├── gui/PvzScreen            — ПВЗ: получение заказов + отправка на продажу
├── gui/BankScreen           — обналичивание баланса в банкноты и обратно
├── gui/JobScreen            — задания от бригадира/бариста
├── gui/widgets              — мелкие UI-хелперы
└── GpsMapRenderer           — NativeImage-карта местности по загруженным чанкам
resources
├── fabric.mod.json, craftnet.mixins.json
├── assets/craftnet/{lang,items,models,blockstates,textures}
└── data/craftnet/{structure,loot_table,recipe}
tools/gen_textures.py        — генерация пиксельных PNG без PIL
tools/gen_structures.py      — ASCII-планы зданий → бинарные NBT
```

## Геймдизайн

### Телефон
- Стартовый предмет при первом входе (также крафт: железо+редстоун+медь+стекло).
- Клавиша `P` или ПКМ по предмету. Приложения: Магазин, Биржа, GPS, Банк (просмотр), Связь.
- Магазин/Биржа требуют связь ≥2G; GPS — спутниковый приём (не глубоко под землёй).

### Связь (уровни от расстояния до ближайшей ОНЛАЙН-деревни)
| Уровень | Радиус | Эффекты |
|---|---|---|
| 4G | ≤ 120 | доставка ×1, биржа да |
| 3G | ≤ 320 | доставка ×2, биржа да |
| 2G | ≤ 640 | доставка ×3, биржа нет (нужен 3G) |
| нет | > 640 | только офлайн-экран телефона |

Деревня онлайн, если её вышка цела (ядро вышки не разрушено).

### Вышка связи
- Ставится автоматически при обнаружении деревни (событие `CHUNK_LOAD` по
  чанку центра — сразу при подходе игрока, без фоновых сканов) на кольце
  40–64 блока от центра, после проверки, что целевые чанки загружены
  (не внутри), процедурная постройка + `tower_core` на вершине (светится).
- Разрушение `tower_core` → деревня офлайн. Починка: установить новое ядро рядом
  с деревней (крафт/покупка в магазине) → деревня снова онлайн.

### Деревенские здания (инъекция в пулы `village/*/houses`, вес 6)
1. **ПВЗ** (`pvz`) — сотрудник со скином странствующего торговца (NoAI, Invulnerable,
   тег `craftnet:pvz`). ПКМ → экран ПВЗ: получение заказов и отправка товаров на продажу.
2. **Банк** (`bank`) — библиотекарь «Банкир» (тег `craftnet:bank`). Обналичивание:
   баланс → предметы «Банкнота» с номиналом (текстуру нарисует заказчик), и обратно.
3. **Завод** (`factory`) — бригадир (тег `craftnet:foreman`): производственные заказы
   (принести компоненты редстоун-тематики), максимальная оплата.
4. **Кафе** (`cafe`) — бариста (тег `craftnet:barista`): заказы на еду, старт курьерки.

### Экономика (модель — ECONOMY.md)
- Валюта «кредит» (CR), баланс на игрока. Обмен курсов нет — 1 CR = 1 CR.
- Магазин: buy-цена из прайса (`PriceManager`, ~190 позиций + fallback-эвристика;
  модель: трудоёмкость добычи, сжатый блок = 9×, **лут-снаряжение = 0.72×материалов**,
  эндгейм-стены: элитра 8000 / звезда 2000 / маяк 2500). Комиссия/спред биржи 2%.
- Продажа игрок→сервер **ступенчатая** (конфиг): ≤9 CR ×0.55, 10–499 ×0.68,
  ≥500 ×0.80, floor — хлам (buy ≤ 2) не принимается вовсе (антилуп «доски→палки»).
- Казино-апгрейдер: шанс = stake(по sell)/цель(по buy) × **RTP 90%** (конфиг) —
  плата за азарт ~11% к магазину на дистанции; петли нет (двойной спред).
- Биржа: дивиденды 0.1–0.4%/сутки (пассив-добавка, не замена работе),
  **лимит портфеля 50 000 CR** (конфиг), потолок выплат с компании 2500.
- Оплата работ (лестница окон, строго не пересекаются): **завод 195–285,
  грузчик 150–190, повар 65–140, курьер 25–50**; темп — одна смена типа на
  10-минутное окно (`jobWindowCooldown`), крафт-заказы платят долю от
  стоимости материалов (формулы и обоснование — ECONOMY.md «Работы»).
- Продажа товаров: оставляем в ПВЗ → выплата приходит с задержкой (как «продал онлайн»).

### Доставка
- База — 2 игровые минуты (2400 тиков) × множитель связи + штраф расстояния.
- Получение — только в ПВЗ деревни, из экрана сотрудника.
- Выплаты за продажу с ПВЗ имеют ту же логистику и видны в ПВЗ лентой
  «Ожидаемые выплаты» (сумма, ETA, прогресс каждой выплаты — поле `start`
  сохраняется в записи выплаты).

### Настройки сервера (config/craftnet.json)
- Все числовые константы систем — в `config/craftnet.json` (`CraftNetConfig`):
  экономика (стартовый бонус, sell-коэффициенты), логистика (время доставки,
  TTL посылок и лотов барахолки, комиссия), работы (неустойки за отмену/срыв
  смены, лестницы оплат всех видов, окно-кулдаун), банк (процент/потолок/
  минимальный баланс), казино (пределы ставки и шансов, RTP), биржа (лимит
  портфеля, потолок дивидендов), связь (скидка «умной вышки»), период
  синхронизации экранов.
- Загружается при старте сервера; отсутствующие ключи дописываются дефолтами,
  значения санитизируются (клампы в разумных пределах) — битый конфиг не роняет
  сервер, а чинится сам.

### GPS 2.0
- Карта 160x160 из загруженных чанков (`NativeImage`, цвет верхнего блока, оттенок
  по высоте). **Зум 1/2/4 блока на пиксель**, пересэмпл по движению (порог зависит
  от зума) и по смене зума — карта следует за игроком.
- Оверлеи: игрок (стрелка yaw), деревни (статус), цель активной работы (jobNav
  из синка телефона), маршрутная метка по клику с дистанцией. Справа — список
  5 ближайших деревень с метражом.
- Под землёй (ниже уровня моря −12 и без неба над головой) — «нет сигнала GPS».

### Работы 2.0 (крафт-заказы + штрафы; курьер ≠ повар)
- Оплата убывает по ТЗ и **строго не пересекается**:
  завод 195–285 > грузчик 150–190 > повар 65–140 > курьер 25–50
  (неустойки/лестницы/формулы — в `craftnet.json`, блок `job*`).
- **Темп — кулдаун окон**: тип смены отрабатывается раз за 10-минутное окно
  (`jobWindowCooldown`, отмена/таймаут = тоже смена, анти-реролл оффера);
  в синке отработанный оффер помечается `cool=1` — кнопка «Принять» на
  экране нанимателя и в ПВЗ заменяется подсказкой «отработано».
  Только завод = 1440 CR/ч ≈ якорь экономики.
- **Цеховой крафт-заказ** (завод, T_FACTORY_ORDER) и **повар** (кафе, T_COOK):
  сервер выдаёт помеченные материалы (компонент `craftnet:job_tag` = «owner:type»
  + имя «◆») ровно по ванильным рецептам выбранных целей (завод: repeater/
  comparator/piston/observer/лампа/dispenser/target/note/dropper/tnt; кафе:
  bread, пирог, стью и суп, печенье, кролик-стю). Сдача — кнопкой «Сдать заказ»: сервер
  проверяет наличие целевых предметов, изымает, платит.
  **Оплата = clamp(материалы × mult/100 + bonus, окно лестницы)**: цех ×100%+90,
  повар ×45%+60 — медианный заказ платит центр окна (константы перебраны
  `tools/simulate_jobs.py`), на карточке оффера видна стоимость выданных
  материалов (≈ N CR).
- **Штрафы за срыв**: отмена 25% от оплаты, таймаут 30% (конфиг, читается
  живьём — работает `/craftnet reload`); недостающие помеченные материалы
  оцениваются по цене продажи и добавляются к штрафу; груз грузчика/курьера
  конфискуется (+`jobCargoLossFee` 80 за ящик цеха). Лазейка
  «взял заказ → забыл/продал → взял новый» закрыта.
- **Мини-игра «сборка по схеме»** остаётся отдельным оффером завода
  (T_FACTORY, 90+35/деталь → 195–285, 3 ошибки = брак).
- **Курьер** — отдельная работа: FOOD_BOX жителю, 10+5×порции(3..8) → 25–50.
  Повар больше не разносит: повар = кухня, курьер = доставка.
- Цели доставок: re-glow каждые 5 с + стрелка-навигатор в HUD (jobNav в
  hud_sync) + оранжевая метка в GPS.

## Статус сборки

✅ CI зелёный: Gradle 9.5.1 + JDK 25 + loom-remap 1.17.19, `craftnet-1.0.0.jar` собирается в GitHub Actions.

- ✅ UI 2.0: «скруглённые» карточки, статус-бар (время/сеть/батарея/баланс),
  звуки кликов, домашний экран-плитки, переводы денег из телефона,
  ПВЗ с пейджингом и прогресс-барами доставки, чипы сумм в банкомате.
- ✅ Фиксы ревью: переполнение строк в магазине/бирже/ПВЗ, 4→3 палочки сигнала.
- ✅ **Барахолка** (маркетплейс): MarketState/MarketManager — лоты из ПВЗ
  (до 6/игрок, цена 1..1e6, TTL 3 игровых дня), покупка из телефона с обычной
  доставкой, комиссия 5% из выплаты продавца, снятие/истечение → почта-возврат.
- ✅ **Казино-апгрейдер 2.0**: ставка — пул в NBT (до 4 видов, до 64 шт.),
  честная формула chance = ΣsellPrice / buyPrice (1..95%). UI — циферблат
  с дугой шанса и стрелкой (6 оборотов, ease-out, тиканье каждые ~12°,
  остановка внутри/вне дуги по решению сервера, детерминированно из spinId),
  чипы автодобора x2/x4/x8/30/50/70% (жадный план по агрегированному
  инвентарю), сетка источника 6x2, выбор цели — отдельный экран с поиском.
  Крупные выигрыши (≤20%) — в общий чат.
- ✅ **Биржа 3.0**: цены дышат постоянно (пульс 5 с, σ по характеру), якорь
  блуждает в именном коридоре компании (VLBK 140–230 спокойный, CRPR 35–150
  дикий...), спайки ±3–9%, новости каждые 2–4 мин с шоком ±6–20%, история
  96 точек по 20 с. Дивиденды — только держателям (защита), каждая выплата
  логируется в консоль сервера.
- ✅ **Здания в деревнях — сразу и гарантированно**: двойной механизм.
  (1) Инжекция джигсо-пулов (TERRAIN_MATCHING, веса pvz/bank 8, factory/cafe 5,
  elementWeights в 1.21.11 иммутабелен — заменяем список целиком @Mutable-миксином)
  — для НОВЫХ деревень. (2) **Программная постановка**: `ServerChunkEvents.CHUNK_LOAD`
  ловит чанк со StructureStart деревни (`village_*`), деревня регистрируется и
  `StructureTemplate.place` ставит все 4 здания с NPC по кольцу r=16..29
  на ровных площадках (проба 9 точек, разброс высот ≤2). Незагруженные чанки
  не пугают: очередь `pendingV` + ретраи (~30 попыток, тик раз в 6 с),
  повторная постановка исключена битовой маской `bmask`. То же для вышки —
  ставится сразу по загрузке чанка, а не фоновым сканом. Валидация шаблонов
  при старте логируется в консоль.
- ✅ **Цены**: торгуются только minecraft:-предметы (+ tower_core); таблица
  покрывает руды-блоки и сжатые блоки (нет арбитража «сено vs алмазы»).
- ✅ **Покрытие сети ×2**: 4G ≤120, 3G ≤320, 2G ≤640.
- ✅ **UI «воздух»**: телефон 300x240, строки 20–22 px, вводы внутри карточек.
- ✅ **Апгрейды вышки** (см. ниже «Вышки: уровни»).
- ✅ **Профиль игрока** (см. ниже «Профиль: статистика и достижения»).

## Вышки: уровни апгрейда (ур. 0..4)

ПКМ по ядру вышки открывает панель прокачки (экран `tower`, TowerScreen).
Уровень хранится в записи деревни (`tlv`) и переживает слом/ремонт ядра.
Оплата — с банковского счёта, эффект мгновенный и общий для всех игроков
(вклад в общественное благо на сервере).

| Уровень | Цена | Радиусы 4G/3G/2G | Перки |
|---|---|---|---|
| 0 Базовый комплект | — | 120/320/640 | — |
| 1 Усилитель | 500 | 162/432/864 | покрытие ×1.35 |
| 2 Фазированная решётка | 1500 | 210/560/1120 | покрытие ×1.75 |
| 3 Дальнобойная мачта | 4000 | 270/720/1440 | ×2.25 · доставка тиром быстрее |
| 4 Умная вышка | 9000 | 330/880/1760 | ×2.75 · доставка ещё быстрее · биржа в 2G · магазин −5% |

Ключевые решения:
- `signalFor` перебирает все онлайн-вышки и берёт **лучшую по тиру** —
  прокачанная дальняя вышка может обгонять базовую ближнюю. Сигнал даёт
  запись обслуживающей деревни (координаты доставки, уровень, дистанция).
- Логистика ур.3–4: `travelMultiplier = max(1, base_tier_mult − bonus)`.
- Визуальный рост мачты: каждый уровень достраивает +2 сегмента решётки
  и поднимает sea-lantern-«огонёк»; ур.3 — крестовина антенн, ур.4 —
  кольцо редстоун-ламп. Достройка идемпотентна (перезапись секции).
- GPS-карта рисует кольца зоны 4G (`r4`, цвет по уровню/офлайну),
  в списке деревень — значки «уN»; HUD показывает «уN» рядом с сетью.

## Профиль: статистика и достижения

`state/StatsState` («craftnet_stats») + `stats/StatsManager`. Все экономики
инкрементят счётчики (`bump/set`) в точках оплаты: работы (по типам и
итого, зарплата, штрафы), заказы/посылки, продажи, барахолка, спины казино,
биржа (покупки/доход/дивиденды), переводы, вложения в вышки.

- **Опыт**: зарплата/10, продажи ~/150 (≤15), спин +1 (+15 за победу),
  ачивки +50..+300. Уровень: `lvl = floor(sqrt(xp/100))` (ур.2 = 400 XP,
  ур.10 = 10000). Повышение — золотое сообщение + звук.
- **Звания** по уровню: Новичок→Житель→Работяга→Специалист→Профи→
  Эксперт→Магнат→Легенда. Чисто престиж.
- **20 достижений** двумя эшелонами. Первый: Первая смена, Мастер цеха,
  Логист, Трудяга, Шопоголик, Торговец, Лудоман, Счастливчик, Инвестор,
  Меценат, Инженер связи, Миллионер. Второй (долгие цели): Стахановец
  (серия 5 смен без срыва) и Марафонец (15), Директор завода (50 смен
  завода), Шеф-повар (40 кафе), Почтмейстер (50 доставок грузчик/курьер),
  Джекпот (приз казино от 2000 CR), Рантье (5000 CR дивидендами),
  Архитектор сети (15 000 CR в вышки). Каждое следит за счётчиками,
  открывается один раз, даёт XP; «Миллионер» освежается из HUD-пульса
  и при открытии профиля. Серия смен `jobsStreak`: +1 за оплаченную
  смену, 0 при отмене/таймауте.
- **Сезонные челленджи**: сезон = игровая неделя (7 × 24000 тиков ≈ 2.3 ч
  реального времени). Пул из 9 челленджей (6 доставок, 6 смен завода/кафе,
  продажи на 1200 CR, 8 спинов, 3 лота барахолки, 4 посылки, 10 любых
  смен, 500 CR переводами); **3 активных выбираются детерминированно по
  номеру сезона** — одинаковы для всего сервера (общая гонка). Прогресс =
  прирост счётчика от базлайна сезона (`rec.ch.b_<key>`: при ротации
  базлайны пересаживаются лениво на первом же изменении статистики или
  открытии профиля). Приз: 300–500 CR + 100–150 XP, ✦-сообщение и звук;
  каждый челлендж выполняется один раз за сезон.
- Телефон: 8-я вкладка «Профиль» (иконка-голова): шапка с именем, уровнем,
  званием и XP-полоской; подвкладки «Сводка» (две колонки: РАБОТА/СВЯЗЬ и
  ЭКОНОМИКА), «Достижения» (сетка 3×4, **пейджинг по 12**: стрелки,
  колесо мыши, PgUp/PgDn) и «Челленджи» (3 карточки активных: условие,
  награда, прогресс-бар, таймер до ротации сезона).

## Реализовано сверх MVP (этапы 1–4)
- ✅ Мини-игра «сборка по схеме» (завод, повар) + фаза разноски блюд с glowing-клиентами.
- ✅ HUD связи (mixin InGameHud): палочки сигнала, деревня/дистанция, GPS-статус, баланс.
- ✅ Периодический `hud_sync` пакет (каждые 2 с), клавиша P.
- ✅ Биржа 2.0: дивиденды раз в игровые сутки всем держателям (и офлайн),
  рыночные события (шок ±6–20%) с лентой новостей и тикером в телефоне.
- ✅ Банк: ежедневный процент на остаток (0.15%, потолок 25 CR).
- ✅ Уведомления: прибытие заказа в ПВЗ (чат+звук), дивиденды/проценты, новости биржи.
- ✅ `/craftnet tutorial` — встроенный гайд (ru/en), приветствие указывает на него.

## Дорожная карта (не входит в текущий объём)
- ИИ жителей в кафе: кастомный Brain-goal (посиделки за столиками) — mixin в
  `VillagerTaskListProvider`, POI типа `meeting` для кафе.
- Падение цен акций при массовых продажах игроков (рыночное давление).
- Страхование посылок, репутация курьера, расширение каталога прайсов из датапаков.
- Кастомные профессии жителей вместо NoAI-NPC (когда заказчик утвердит текстуры).
- Текстура банкноты — от заказчика.

## Известные точки риска — ИТОГ ПРОВЕРКИ (2026-08-24, по yarn-javadoc 1.21.11+build.6)
1. `TextureManager.registerTexture(Identifier, AbstractTexture)` — **есть**, ровно с этой сигнатурой.
2. `Registry#getOrThrow(TagKey)` — **есть** (наследуется от `RegistryEntryLookup`).
3. `getPersistentStateManager()` на ServerWorld — **есть**, возвращает `PersistentStateManager`.
4. `RenderPipelines.GUI_TEXTURED`, `DrawContext.drawTexture(RenderPipeline, Identifier, x, y, u, v, w, h, tw, th)` — **есть**.
5. `NativeImageBackedTexture(Supplier<String>, int, int, boolean)` + `upload()` — **есть**.
6. DataVersion в NBT-структурах — **4671** (официальное значение 1.21.11), поправлено.
7. `Screen.render` с 1.20.2 вызывает `renderBackground` (blur+darkening) — **учтено**:
   `CraftNetScreen.renderBackground` переопределён светлой заливкой без блюра,
   контент рисуется ПОСЛЕ `super.render`.
8. **`ServerWorld.getEntity(UUID)` переименован в `getEntityAnyDimension(UUID)`** — учтено (JobManager).
9. Удобный `ServerWorld.locateStructure(TagKey<Structure>, BlockPos, int, boolean)` — **используем его**
   вместо ручного `ChunkGenerator.locateStructure` (VillageManager).
10. `StructurePool.elements` (`ObjectArrayList`) и `elementWeights` (`List<Pair>`) — **подтверждены**,
    миксин-аксессор и инжектор на них опираются.
