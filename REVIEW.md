# CraftNet — полное ревью кода, геймплея и UI

> Дата: 2026-08-25. Объём: ~7 900 строк Java + ресурсы. Прочитан каждый файл:
> все менеджеры, сетевой слой, клиентские экраны, миксины, хелперы, ресурсы.
> Оценки: **HIGH** — чинить немедленно, **MEDIUM** — в ближайший апдейт, **LOW** — полировка.

---

## 1. Вердикт коротко

Код зрелый: чистое разделение сервер/клиент, всё критичное валидируется на сервере,
состояния — через PersistentState, сеть — через 4 типизированных payload'а, UI —
через единственную дизайн-систему UiKit. Это не «студенческий мод», это аккуратная
продакшн-структура. Но найдено **3 комплекса проблем экономики/античита** и ряд
UX-нестыковок. Ниже — всё по пунктам.

---

## 2. Что сделано хорошо (зафиксировать как стандарт)

1. **Серверная валидация почти везде**: покупки, спины, офферы работ пересоздаются
   детерминированным зерном — клиент не может подделать параметры задания.
2. **PersistentState-менеджеры** с паттерном «getCompound → КОПИЯ → put обратно»
   соблюдён дисциплинированно (Nbt2-хелперы). Гонок/потерь данных не видно.
3. **UiKit + палитра** — единый стиль всех экранов; LIFO-приоритет кликабельных
   регионов; вводы с scissor-клипом и кареткой.
4. **Анимации казино** (плавная дуга, циферблат Брезенхемом, пульс кнопки,
   вспышка ставки) — лучший UX-момент мода.
5. **Журналирование**: дивиденды, уровни, ачивки, вышки пишутся в лог сервера —
   настоящий аудит-трейл.
6. **Анти-регрессии в комментариях**: места, где API 1.21.11 кусается
   (иммутабельный elementWeights, getCompound-копии), помечены прямо в коде.

---

## 3. Находки HIGH (эксплойты/баги)

### H1. Рабочие материалы (◆ job_tag) продаются — фарм-машина
`JOB_TAG`-стаки (материалы цеха/кафе, ящик грузчика, пакеты курьера) можно:
- **продать в ПВЗ** (`fillPvzSync`/`handlePvz.sell` агрегируют без фильтра по тегу);
- **выставить лотом на барахолку** (`MarketManager.create`);
- **поставить в казино** (`addStake`, `buildCasinoSync.src`).

Комбо-эксплойт: принять цеховой заказ → продать выданные ◆материалы → отменить
смену. Неустойка за недостачу списывается как `min(баланс, штраф)` — **при нулевом
балансе штраф = 0**, а выручка от продажи приходит через 2 минуты. Чистый доход
за клик, повторять бесконечно.
**Фикс**: хелпер `JobManager.isJobTagged(ItemStack)`; запрет в `handlePvz.sell`,
`MarketManager.create`, `CasinoManager.addStake` + скрытие из src/sell-списков.
Плюс: при успешной сдаче крафт-заказа **конфисковать остатки ◆ материалов**
(`handin`: removeTagged для всех mats) — иначе склад обдирается легально.

### H2. Действия стоек без проверки дистанции + «open» любого экрана
- `handlePvz.sell`, `handleBank.*`, `loader_start`, `market_list` **не проверяют**
  координаты станции из `OpenCtx.station` (проверка 16 м есть только у вышки).
  Кастомный клиент продаёт/обналичивает из любой точки мира.
- `case "open"` принимает **любой** id экрана и ставит станцию 0 — т.е. можно
  открыть «pvz»/«bank» без стойки вообще.
**Фикс**: whitelist экранов для `open` (только `phone`); для станционных действий
требовать `ctx.station != 0` и `dist² ≤ 24²` (сразу заборчик против пакет-ботов).

### H3. Телефон открывается без телефона
Клиентская кнопка P и серверный `open phone` не проверяют наличие `PHONE`
в инвентаре. Крафт/выдача телефона теряют смысл; после смерти без телефона
всё равно полный доступ к магазину/бирже.
**Фикс**: в `requestOpenPhone` проверять инвентарь на клиенте (подсказка
«нет телефона») + серверная проверка в `openScreen("phone")` (анти-пакет).

---

## 4. Находки MEDIUM

| # | Место | Проблема | Что делать |
|---|-------|----------|------------|
| M1 | `VillageManager.onTowerCoreBroken` | ветка `nearTower` ставит `off=1` деревне **без вышки** (ty<0); `ensureTower` потом строит вышку, но `off` не сбрасывает → **вечный офлайн** | `ensureTower`: `off=0`; пересмотреть условие `nearTower` (ставить off только если ty<0 И ядро было соседом-подключением) |
| M2 | `AdminCommands` + `PvzScreen` | ПВЗ советует «/craftnet job cancel», но команда требует **OP + аргумент player**. Принял грузчика на ПВЗ → отменить нельзя вообще (экран работ только у завода/кафе), выход — 10 мин до таймаута со штрафом | добавить `/craftnet job cancel` себе без OP; кнопку «Отменить смену» в ПВЗ-полосе работы |
| M3 | `handin` (JobManager) | сдача забирает **любые** предметы цели, а остатки ◆-выданных не изымаются → склад обдирается | при OK — `removeTagged` всех mats до фикса H1 |
| M4 | `handleBank.cashout` | выдаёт **по 1 банкноте за стак** в цикле: снятие 10 000 → 10 отдельных стаков, 100 000 → 100 стаков (дроп на пол) | складывать до 64: `ofValue(d, n)` |
| M5 | `OrderManager` | DELIVERY-заказы **никогда не чистятся** (только по claim) — если игрок не заберёт, `orders` растёт вечно | TTL (напр. 5 игровых дней) + лог/возврат денег или drop |
| M6 | `GpsMapRenderer` | создаётся **на каждый PhoneScreen** → повторная `registerTexture` одного и того же id (утечка GPU-текстуры при каждом открытии телефона) | статический синглтон |
| M7 | `buildOffer/accept` | оффер пересоздаётся по зерну «uuid ^ время/12000»: если 10-минутное окно перевалило между показом и кликом «Принять», игрок получает **другое** задание/оплату | показывать «обновится через m:ss» или принимать слепок оффера (подписанный сервером) |

---

## 5. Находки LOW

1. `tickCarryWeight`: снимает **чужой** Slowness (зелья/мобы), глядя только на наличие эффекта — проверять amplifier==1/duration≤80 или помечать наш эффект.
2. `CraftNetHud.updatedTick` — мёртвое поле (не используется).
3. `pushHudSync`: два запроса `MoneyManager.balance` подряд; косметика.
4. `StocksManager.price(id)`: `toUpperCase()` без `Locale.ROOT`.
5. Sync-трафик: история биржи (96 int × 5 компаний) летит каждые 0.5 с каждому с телефоном — можно hist раз в 5 с, а `tickOpenScreens` раз в 1 с.
6. `UiKit.TextInput`: нет вставки Ctrl+V, Home/End, стрелок — минимализм ок, но вставку хотелось бы (имена игроков).
7. `lockOverlay` для офлайн-GPS пишет «связь дают вышки рядом с деревнями» — нерелевантно под землёй (нужна своя подсказка «поднимись на поверхность»).
8. ПВЗ-лента выплат: при сумме >99 999 строка «всего N CR» может визуально налезть на «и ещё N».
9. `MarketManager/clientRows`: `List.copyOf`-копии лотов каждый sync — O(N) при N лотов; при сотнях лотов + 0.5-с синком заметно (см. Perf).
10. `PhoneScreen` магазин: `buyCount` степени двойки (1..64) — нет «макс по балансу».
11. `ensureDefaults` (Stocks): общий флаг dirty перетекает между итерациями (безвредно, но некрасиво).
12. `VillageStructureInjector` в javadoc ссылается на `END_DATA_PACK_CONTENTS_RELOAD` — реальное имя `END_DATA_PACK_RELOAD` (комментарий устарел).
13. Банк-чипы `+10/+100/+1000` пишут в поле в обход `maxLen=9` (безобидно; сервер клампит 100 000).
14. HUD не гаснет при обрыве пушей (нет stale-check по `updatedTick`).

---

## 6. Детальные заметки по системам

### Деревни / вышки / генерация
- CHUNK_LOAD → очередь → регистрация → постановка вышки и 4 зданий с NPC — корректно
  и идемпотентно (битмаска `bmask`, `bld`, ретраи 30×6 с). Миграция старых миров
  покрыта (bld=0 → достройка).
- `placeVillageBuildings`: площадка проверяется ±4 м, здание до 11 м — края могут
  висеть над склоном. Косметика; можно зондировать по факту футпринта.
- `buildTower` ходит по 12 кандидатам на кольце 44–56 м, с проверкой isChunkLoaded —
  хороший компромисс. Платформа 3×3 может выйти на соседний чанк (±1 блок) — микро-риск
  лишнего тяг-чанка, терпимо.
- Джигсо-инъекция как «бонус» валидна: TERRAIN_MATCHING, двойное покрытие
  weights/elements, лог шаблонов. Опасность роста «внутри улиц» минимальна.
- **`towerView` и `upgradeTower` чисты**: деньги списаны → уровень вырос → мачта
  достроена идемпотентно. Публичное сообщение — приятный момент.

### Связь / сигнал
- «Лучшая вышка по тиру, tie→ближняя» — честная и предсказуемая модель; офлайн-
  деревня показывается особо. Радиусы и множители логистики сведены в таблицы — легко
  балансить. **Ок.**

### Заказы / ПВЗ / доставка
- ETA и прогресс считаются из `start/ready` — теперь видны игроку (лента).
- `claim` — единственная стойка с проверкой дистанции (96 м) — эталон, размножить (H2).
- **M5 (вечные доставки)** — главный хламотворец стейта.
- Стак >64 по заказу ложится одним инвалидным стаком — ваниль терпит; хопперы/дроп
  могут повести себя неожиданно. Можно разбивать при выдаче.

### Работы (JobManager)
- Лестница оплат 195-285/150-190/110-145/25-50 не пересекается — годно.
- Мини-игра сборки: детерминированные последовательности, 3 ошибки = пересбор —
  хорошо; seq светится клиенту в открытую (автокликеры возможны — некритично за 200 CR).
- `pickTarget` берёт жителей в радиусе 96×48×96 вокруг центра — на краю прогрузки
  оффер «исчезает». Дать в UI причину («цели кончились, подойди ближе»).
- **H1/M3 лечатся вместе** — после этого экономика работ герметична.
- Штраф min(баланс, штраф) оставляет возможность «банкротства без долгов» —
  после H1 это не эксплойт, а фича (мягкое обучение новичка).

### Магазин / цены
- Каталог кэшируется, фильтр по имени/id, цены: 150+ позиций таблицей + эвристика
  rarity/stack. Арбитраж крафтов местами возможен (эвристики не знают рецепты) —
  стейки мелкие, терпимо; зафиксировано прайс-таблицей для остроумных случаев.
- Анти-p2w: внешние моды не продаются (namespace-фильтр). **Ок.**

### Барахолка
- Комиссия 5% из выплаты, TTL 3 дня, возврат почтой — продумано.
- Лот создаётся на **всё количество** предмета (до 64) — нет выбора количества;
  хочется чипы 1/8/32/всё.
- Пагинация 6/стр ✓. Покупка → двойная доставка (покупателю/продавцу) атомарна.

### Казино
- RTP = sell/buy = 72% — документированно-честное «честное казино» дом выигрывает.
- Спин серверный, клиент — театр по seed ✓. Пресеты/чипы/шаги — новый UX выглядит сильно.
- `bp<1%` блокируется. Ок после фикса H1 (ставить чужие ◆ нельзя будет).

### Биржа
- Пульс/якоря/спайки/новости — живой рынок; спред 2% гасит скальпинг.
- Дивиденды с guard'ом и логом; фантом-инцидент надо проверить по логу при повторении.

### Банк / деньги
- Процент 0.15%/день от 50 CR, кап 25 — ненавязчивый стимул держать капитал.
- tx-log 16 записей ✓. Переводы только онлайн — осознанное ограничение.
- Обналичивание M4 — единственный неприятный UX.

### Профиль / ачивки
- 12 достижений покрывают все подсистемы; xp = 100·n² — ровная кривая; левел-ап
  и звёздочка в чат — приятно. «Миллионер» из pushHudSync-пульса — легальная гонка
  (set() с early-return от спама). **Ок.**

---

## 7. UI/UX ревью

**Сильное**: единая палитра; статус-бар (время/сеть/баланс); 8 иконок-вкладок;
lockOverlay для фич по сети; кольца 4G на карте с клипом; профиль с сеткой ачивок;
лайв-дуга шанса; шаг «причины» прямо на кнопке спина.

**Слабое (по убыванию боли):**
1. Отмену работы грузчика из ПВЗ невозможно выполнить (M2) — **человек застревает**.
2. Перевод: имя игрока вводится вслепую, без списка онлайна/автокомплита.
3. Лот барахолки — «всё количество сразу», продажа в ПВЗ — тоже только «всё».
4. Пейджеры только кнопками — колесо мыши не листает.
5. Цена лота: поле ввода спрятано в шапке колонки ПВЗ — неочевидно, что это за число.
6. Длинные числа (баланс 6+ знаков) сжимают подписи (лента выплат, статус-бар).
7. lockOverlay для GPS-офлайна — нерелевантная подсказка (L7).

---

## 8. Производительность и сеть

- `tickOpenScreens` (0.5 с): ~4–8 КБ синка на игрока (shop-page + market + casino src +
  stocks с историями + profile). 20 игроков ≈ 160 КБ/с — приемлемо, но есть куда резать
  (hist реже, sync 1 с).
- Фильтр каталога — O(N~1500) строковых сравнений на игрока/синк: копеечно для CPU,
  но приятно прячется в кэшированной алфавитной выборке.
- Стейт-листы (`orders`, `lots`) перезаписываются целиком при мутации (копия) —
  O(N) на операцию; при тысячах записей и M5-раздувании ощутимо.
- GPS-resample: до 25 тыс. `getTopY` каждые 10 тиков на клиенте — норм (heightmap O(1)).

---

## 9. План фиксов (предлагаемый порядок)

1. **H1+H2+H3** одним коммитом: tag-фильтр продаж/ставок/лотов + конфискация при сдаче;
   whitelist `open`; проверка дистанции станции для sell/bank/loader/market_list;
   серверная проверка телефона.
2. **M1** (`off` при достройке вышки) — 2 строки, критично для мира.
3. **M2** (`/craftnet job cancel` себе + кнопка в ПВЗ).
4. **M4** (стаки банкнот), **M5** (TTL доставок), **M6** (синглтон карты).
5. LOW-пак по вкусу: автокомплит перевода, колёсико-листание, чипы количества лота.

*Этот документ не меняет код — только ревью. Фиксы — отдельными коммитами по согласованию.*
